# OS
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# Editors
.vscode/
.idea/
*.swp

# Machine-local Claude Code permissions
.claude/settings.local.json

# Nothing here builds, but keep these out if tooling arrives later
node_modules/
dist/

# Chain casino SDK local dev/test stack (simulator, coinflip example, docs) —
# reference tooling only, not part of the submission; re-download from
# sdk.chain.wtf/casino if missing.
/casino-sdk/

# Stale download artifact, locked on Windows — see if it can be removed later
.sdk-download/
